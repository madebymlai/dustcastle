{
  "name": "sample",
  "version": "0.0.0",
  "private": true,
  "description": "dustcastle pnpm e2e fixture (slice 2b) — one real dep + a built-in node:test.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "is-number": "7.0.0"
  }
}
