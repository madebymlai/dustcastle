{
  "name": "sample",
  "version": "0.0.0",
  "private": true,
  "description": "dustcastle pin-then-pure e2e fixture — a loose manifest (no lockfile).",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "is-number": "7.0.0"
  }
}
